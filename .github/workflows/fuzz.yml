name: Fuzzing

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: '23 7 * * *'  # Run fuzzing daily at 07:23 UTC

permissions:
  contents: read

jobs:
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955  # v4.3.0
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@44454db4f0199b8b9685a5d763dc37cbf79108e1  # 2.36.0
        with:
          php-version: '8.4'
          extensions: mbstring, sodium
          coverage: none

      - name: Setup PHP-Fuzzer
        run: composer global require nikic/php-fuzzer

      - name: Install dependencies
        uses: ramsey/composer-install@3cf229dc2919194e9e36783941438d17239e8520  # v3.1.1
        with:
          composer-options: "--optimize-autoloader"

      - name: Run FuzzMerkleTree
        run: php-fuzzer fuzz --max-runs 50000 fuzzing/FuzzMerkleTree.php

      - name: Run FuzzEncoding
        run: php-fuzzer fuzz --max-runs 50000 fuzzing/FuzzEncoding.php

      - name: Run FuzzProtocol
        run: php-fuzzer fuzz --max-runs 50000 fuzzing/FuzzProtocol.php

      - name: Run FuzzAttributeEncryption
        run: php-fuzzer fuzz --max-runs 50000 fuzzing/FuzzAttributeEncryption.php
